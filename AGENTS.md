# PROJECT KNOWLEDGE BASE**Generated:** 2026-07-17**Status:** Development Phase (MVP complete)## OVERVIEWGo-WHOIS is a domain WHOIS/RDAP lookup service. CLI + HTTP API, JSON output. Stack: cobra (CLI) + gin (HTTP) + viper (config) + zap (logs).## STRUCTURE (Implemented)```go-whois/├── cmd/                  # CLI entry points (root, lookup, serve)├── internal/             # Private business logic�?  ├── config/           # Config structs + loader�?  ├── service/          # LookupService (query orchestrator)�?  ├── engine/           # RDAP + WHOIS engines�?  ├── cache/            # Memory/Redis cache�?  ├── api/              # HTTP handlers, middleware, router�?  ├── model/            # Domain, request, response structs�?  └── errors/           # Custom error types├── pkg/                  # Reusable utilities (validator, tld, netutil)├── config/               # YAML configs + TLD server mappings├── config/                 # RDAP bootstrap JSON├── scripts/              # Build scripts└── test/                 # Fixtures + integration tests```## WHERE TO LOOK| Task | Location | Notes ||------|----------|-------|| Domain validation | `pkg/validator/domain.go` | Regex: `^([a-zA-Z0-9]([a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$` || TLD extraction | `pkg/tld/tld.go` | Parse TLD from domain string || WHOIS servers | `config/tld_whois_servers.yaml` | `.com` �?`whois.verisign-grs.com` || RDAP bootstrap | `config/rdap_bootstrap.json` | IANA official data || Query orchestration | `internal/service/lookup.go` | LookupService decides protocol, calls engine || Engine interface | `internal/engine/engine.go` | `Engine` interface: `Query(ctx, domain) �?DomainInfo` || Result normalization | `internal/engine/normalizer.go` | WHOIS text �?JSON, RDAP JSON �?standardized || HTTP handlers | `internal/api/handler.go` | `Lookup()`, `BatchLookup()`, `HealthCheck()` || CLI commands | `cmd/lookup.go`, `cmd/serve.go` | cobra commands || Error codes | `internal/errors/errors.go` | `INVALID_DOMAIN`, `DOMAIN_NOT_FOUND`, `QUERY_TIMEOUT`, etc. || Config loading | `internal/config/loader.go` | viper with env var prefix `WHOIS_` |## CONVENTIONS- **Go version**: 1.21+- **Package naming**: lowercase, single word (`engine`, `cache`, `validator`)- **File naming**: lowercase + underscore (`lookup_service.go`, `rdap_bootstrap.go`)- **Interface naming**: verb + noun (`Engine`, `CacheManager`, `Normalizer`)- **Error handling**: Custom `AppError` with code, message, HTTP status- **Logging**: Structured zap logs with fields: `domain`, `protocol`, `request_id`, `duration_ms`- **Config env vars**: `WHOIS_` prefix, uppercase, underscore for dots (`WHOIS_SERVER_PORT`)- **Imports**: stdlib �?third-party �?internal (`go-whois/internal/...`)## ANTI-PATTERNS (THIS PROJECT)- **DO NOT** expose `internal/` packages �?they're private by Go convention- **DO NOT** hardcode WHOIS servers �?use `config/tld_whois_servers.yaml`- **DO NOT** cache errors (except domain not found)- **DO NOT** suppress protocol auto-switch in `auto` mode �?it's a core feature- **DO NOT** use `fmt.Println` for output �?use structured zap logs- **DO NOT** skip domain validation before queries- **NEVER** return raw WHOIS text to API consumers �?always normalize to JSON## UNIQUE STYLES- **Protocol priority**: RDAP first, WHOIS fallback (configurable per TLD)- **Cache key format**: `{protocol}:{normalized_domain}`- **Batch queries**: Max 50 domains, concurrent execution, per-domain error isolation- **Rate limiting**: 100 req/s default, configurable- **Metrics**: Prometheus format at `/metrics`## COMMANDS```bash# CLI usagego-whois lookup example.com                    # Single querygo-whois lookup --protocol rdap example.com    # Force RDAPgo-whois lookup --file domains.txt             # Batch from filego-whois serve                                 # Start HTTP server# Developmentgo build -o bin/go-whois .                     # Buildgo test ./...                                  # Run testsgo test -short ./...                           # Unit tests onlygo test -coverprofile=coverage.out ./...       # Coveragegolangci-lint run                              # Lint```## DEPENDENCIES| Package | Purpose ||---------|---------|| `github.com/spf13/cobra` | CLI framework || `github.com/gin-gonic/gin` | HTTP framework || `github.com/spf13/viper` | Config management || `go.uber.org/zap` | Structured logging || `gopkg.in/natefinish/lumberjack.v2` | Log rotation || `github.com/stretchr/testify` | Test assertions |## NOTES- **MVP Complete**: Core functionality implemented - RDAP/WHOIS query, CLI, HTTP API- **RFC compliance**: WHOIS (RFC 3912), RDAP (RFC 7480-7483)- **Protocol switching**: RDAP 5xx/timeout �?auto-switch to WHOIS (only in `auto` mode)- **WHOIS parsing**: Format varies by TLD �?normalizer must handle edge cases- **Privacy**: Some fields redacted by registrars �?mark as `REDACTED FOR PRIVACY`## THIRD-PARTY LIBRARY USAGEGo-WHOIS 可以作为第三方库使用，提供以下公�?API�?### 包结�?```pkg/├── model/          # 公共数据模型�?  └── domain.go   # DomainInfo, LookupRequest, LookupResponse �?└── whois/          # 公共客户�?API    ├── client.go     # 高级客户端（支持缓存、自动协议选择�?    ├── rdap.go       # RDAP 查询实现    ├── whois.go      # WHOIS 查询实现    └── bootstrap.go  # RDAP Bootstrap �?WHOIS 服务器信息获�?```### 快速开�?```gopackage mainimport (    "fmt"    "log"    "github.com/suguer/go-whois/pkg/whois")func main() {    // 创建客户�?    client := whois.NewClient()    defer client.Close()    // 查询域名    result, err := client.Lookup("example.com")    if err != nil {        log.Fatal(err)    }    fmt.Printf("域名: %s\n", result.DomainName)    fmt.Printf("注册�? %s\n", result.RegistrarName)    fmt.Printf("到期日期: %v\n", result.ExpirationDate)}```### 配置选项```goclient := whois.NewClient(    whois.WithProtocol(model.ProtocolRDAP),       // 设置查询协议    whois.WithTimeout(15 * time.Second),          // 设置超时时间    whois.WithCache(true, 1000, time.Hour),       // 启用缓存    whois.WithRDAPBootstrap("https://custom-url"), // 自定�?RDAP Bootstrap    whois.WithUserAgent("my-app/1.0"),            // 设置 User-Agent    whois.WithRawResponse(true),                  // 包含原始响应)```### 单独使用 WHOIS 客户�?```gowhoisClient := whois.NewWHOISClient(    whois.WithWSTimeout(10 * time.Second),    whois.WithWSPort(43),    whois.WithWSServers(map[string]string{        ".com": "whois.verisign-grs.com",        ".org": "whois.publicinterestregistry.org",    }),)result, err := whoisClient.Query(ctx, "github.com")```### 自定义日�?```gotype MyLogger struct{}func (l *MyLogger) Debug(msg string, keysAndValues ...interface{}) {    // 自定�?debug 日志}func (l *MyLogger) Info(msg string, keysAndValues ...interface{}) {    // 自定�?info 日志}func (l *MyLogger) Warn(msg string, keysAndValues ...interface{}) {    // 自定�?warn 日志}func (l *MyLogger) Error(msg string, keysAndValues ...interface{}) {    // 自定�?error 日志}client := whois.NewClient(    whois.WithLogger(&MyLogger{}),)```### 数据模型- `model.DomainInfo`: 域名查询结果- `model.QueryProtocol`: 查询协议类型 (rdap/whois/auto)- `model.LookupRequest`: 查询请求- `model.LookupResponse`: 查询响应- `model.Error`: 错误信息### 获取 RDAP Bootstrap 数据```go// �?IANA 获取 RDAP Bootstrap 数据rdapEndpoints, err := whois.DownloadRDAPConfig("https://data.iana.org/rdap/dns.json", "")if err != nil {    log.Fatal(err)}fmt.Printf("获取�?%d �?TLD �?RDAP 端点\n", len(rdapEndpoints))for tld, endpoint := range rdapEndpoints {    fmt.Printf(".%s -> %s\n", tld, endpoint)}```### 获取 WHOIS 服务器信�?```go// �?IANA 获取 TLD 列表tlds, err := whois.FetchTLDList()if err != nil {    log.Fatal(err)}// 获取单个 TLD �?WHOIS 服务�?server := whois.FetchWhoisServer("com")fmt.Printf(".com WHOIS 服务�? %s\n", server)// 批量获取 WHOIS 服务�?results := whois.FetchWhoisServers(tlds, 20, func(progress, total int) {    fmt.Printf("\r进度: %d/%d", progress, total)})// 转换�?map 格式serversMap := whois.GetWhoisServersMap(results)// 格式化为 YAMLyamlContent := whois.FormatWhoisServersYAML(results)```### 运行示例```bashgo run examples/usage.go```